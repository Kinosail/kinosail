services:
  kinosail:
    devices:
      - /dev/dri:/dev/dri
      - /dev/dma_heap:/dev/dma_heap
      - /dev/mpp_service:/dev/mpp_service
      - /dev/rga:/dev/rga
    group_add:
      - "${KINOSAIL_GPU_GROUP_1:-10001}"
      - "${KINOSAIL_GPU_GROUP_2:-10001}"
