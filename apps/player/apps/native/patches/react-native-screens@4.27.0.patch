diff --git a/ios/tabs/host/RNSTabsHostComponentView.mm b/ios/tabs/host/RNSTabsHostComponentView.mm
--- a/ios/tabs/host/RNSTabsHostComponentView.mm
+++ b/ios/tabs/host/RNSTabsHostComponentView.mm
@@ -421,7 +421,7 @@
 {
   _layoutDirection = layoutDirection;
 #if RNS_IPHONE_OS_VERSION_AVAILABLE(17_0)
-  if (@available(iOS 17.0, *)) {
+  if (@available(iOS 17.0, tvOS 17.0, *)) {
     _controller.traitOverrides.layoutDirection = _layoutDirection;
   } else
 #endif // RNS_IPHONE_OS_VERSION_AVAILABLE(17_0)
diff --git a/ios/gamma/scroll-to-top-guard/RNSScrollToTopGuardGestureRecognizer.mm b/ios/gamma/scroll-to-top-guard/RNSScrollToTopGuardGestureRecognizer.mm
--- a/ios/gamma/scroll-to-top-guard/RNSScrollToTopGuardGestureRecognizer.mm
+++ b/ios/gamma/scroll-to-top-guard/RNSScrollToTopGuardGestureRecognizer.mm
@@ -59,6 +59,18 @@
   [view addGestureRecognizer:[RNSScrollToTopGuardGestureRecognizer new]];
 }
 
+#else
+
++ (BOOL)shouldGuardScrollToTop
+{
+  return NO;
+}
+
++ (void)applyToView:(nonnull UIView *)view
+{
+  // This platform has no scroll-to-top-on-header interaction to guard.
+}
+
 #endif // RNS_IPHONE_OS_VERSION_AVAILABLE(26_0)
 
 @end
