// Compiler-only declarations for React Native/Expo runtime globals and feature-detected web APIs.
// Passed through hermesc -include-globals; this file is never executed or bundled.
var AbortSignal, Blob, DOMException, ErrorUtils, FileReader, FormData, Headers;
var Image,
  Promise,
  REACT_NAVIGATION_DEVTOOLS,
  ReadableStream,
  Request,
  Response;
var ResizeObserver,
  TextDecoder,
  TextEncoder,
  TransformStream,
  URL,
  URLSearchParams;
var WritableStream, XMLHttpRequest, alert, atob, btoa, cancelAnimationFrame;
var cancelIdleCallback, clearImmediate, clearInterval, clearTimeout, fetch;
var nativeFabricUIManager,
  performance,
  requestAnimationFrame,
  requestIdleCallback;
var setInterval, setTimeout;
