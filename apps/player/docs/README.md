# Kinosail documentation source

GitHub Pages can publish this directory with Jekyll. Public pages use front matter and the shared `default` layout.

Run the local documentation checks before publication. See [the documentation guide](contributing/documentation.md) for page rules and screenshot replacement steps.
