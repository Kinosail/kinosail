name: Dashboard Release

on:
  push:
    tags: ["dashboard-v*"]

permissions:
  contents: read

concurrency:
  group: dashboard-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  quality:
    uses: ./.github/workflows/quality.yml

  release:
    needs: quality
    name: Publish signed Dashboard container
    runs-on: ubuntu-latest
    permissions:
      actions: read
      attestations: write
      contents: write
      id-token: write
      packages: write
    defaults:
      run:
        working-directory: apps/dashboard
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Require enabled gates
        run: test ! -e ../../.gates-disabled
      - id: version
        name: Validate and split Dashboard release tag
        run: |
          if [[ ${#GITHUB_REF_NAME} -gt 80 || "$GITHUB_REF_NAME" != dashboard-v* ]]; then
            printf 'invalid Dashboard release tag: %s\n' "$GITHUB_REF_NAME" >&2
            exit 1
          fi
          version="${GITHUB_REF_NAME#dashboard-}"
          if [[ ! "$version" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
            printf 'Dashboard release tag must be dashboard-vMAJOR.MINOR.PATCH\n' >&2
            exit 1
          fi
          container_version="${version#v}"
          major="${container_version%%.*}"
          remainder="${container_version#*.}"
          minor="${remainder%%.*}"
          printf 'version=%s\ncontainer_version=%s\nmajor_minor=%s.%s\n' \
            "$version" "$container_version" "$major" "$minor" >>"$GITHUB_OUTPUT"
      - name: Require successful Dashboard Hygiene run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          commit="$(git rev-parse "$GITHUB_SHA^{commit}")"
          conclusion="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow dashboard-hygiene.yml --commit "$commit" --event push --limit 1 --json conclusion --jq '.[0].conclusion')"
          if [[ "$conclusion" != success ]]; then
            printf 'tagged commit %s does not have a successful Dashboard Hygiene run\n' "$commit" >&2
            exit 1
          fi
      - name: Require successful security checks on main
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          commit="$(git rev-parse "$GITHUB_SHA^{commit}")"
          git merge-base --is-ancestor "$commit" origin/main
          conclusion="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow security.yml --commit "$commit" --event push --limit 1 --json conclusion --jq '.[0].conclusion')"
          [[ "$conclusion" == success ]]
      - uses: docker/setup-qemu-action@99012661954931238ded8c8b007157a8430204e1 # v4
        with:
          image: docker.io/tonistiigi/binfmt@sha256:400a4873b838d1b89194d982c45e5fb3cda4593fbfd7e08a02e76b03b21166f0
      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4
      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: metadata
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: ghcr.io/kinosail/kinosail-dashboard
          tags: |
            type=raw,value=${{ steps.version.outputs.container_version }}
            type=raw,value=${{ steps.version.outputs.major_minor }}
            type=raw,value=latest
      - id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: apps/dashboard/Containerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/kinosail/kinosail-dashboard:candidate-${{ github.sha }}
          labels: ${{ steps.metadata.outputs.labels }}
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
            REVISION=${{ github.sha }}
          cache-from: type=gha,scope=dashboard-release-container
          cache-to: type=gha,mode=max,scope=dashboard-release-container
          sbom: true
          provenance: mode=max
      - name: Scan candidate Dashboard image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ghcr.io/kinosail/kinosail-dashboard@${{ steps.build.outputs.digest }}
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: "1"
      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
      - name: Sign Dashboard image digest
        env:
          IMAGE_DIGEST: ${{ steps.build.outputs.digest }}
        run: cosign sign --yes "ghcr.io/kinosail/kinosail-dashboard@$IMAGE_DIGEST"
      - name: Attest Dashboard image provenance
        if: ${{ !github.event.repository.private }}
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4
        with:
          subject-name: ghcr.io/kinosail/kinosail-dashboard
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
      - name: Promote verified image to release tags
        env:
          IMAGE_DIGEST: ${{ steps.build.outputs.digest }}
          IMAGE_TAGS: ${{ steps.metadata.outputs.tags }}
        run: |
          args=()
          while IFS= read -r tag; do
            [[ -z "$tag" ]] || args+=(--tag "$tag")
          done <<<"$IMAGE_TAGS"
          docker buildx imagetools create "${args[@]}" "ghcr.io/kinosail/kinosail-dashboard@$IMAGE_DIGEST"
      - name: Publish Dashboard release
        env:
          APP_VERSION: ${{ steps.version.outputs.version }}
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: gh release create "$RELEASE_TAG" --verify-tag --generate-notes --title "Kinosail Dashboard $APP_VERSION"
